import { Routes } from '@angular/router';
import { ServiceManagerPageComponent } from './pages/service-manager-page.component';

export const SERVICE_MANAGER_ROUTES: Routes = [{ path: '', component: ServiceManagerPageComponent }];
